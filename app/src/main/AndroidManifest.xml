<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.a21vianet.sample.customview">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".xiaomiparallax.XiaomiParallaxActivity">
        </activity>
        <activity android:name=".scoreview.ScoreViewActivity">
        </activity>
        <activity android:name=".addshop.AddShopActivity">
        </activity>
        <activity android:name=".countdown.CountdownActivity">
        </activity>
        <activity android:name=".custombehavior1.CustomBehavior1Activity">
        </activity>
        <activity android:name=".pullview.PullViewActivity">
        </activity>
    </application>

</manifest>